CAOM
==================

This is a dev version of the CAOM DM that generates all [code](generated/javadoc) and [schema](schema.md) from the
VO-DML definition using the [vodmltools](https://ivoa.github.io/ProposalDM/).
